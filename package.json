{
  "name": "property-rent-management-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.2"
  },
  "dependencies": {
    "express-validator": "^7.3.2",
    "multer": "^2.2.0"
  }
}
